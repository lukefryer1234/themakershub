{
  "name": "themakershub",
  "version": "1.0.0",
  "scripts": {
    "spec:edit": "node -e \"require('spec-up')()\"",
    "spec:build": "node -e \"require('spec-up')()\""
  },
  "devDependencies": {
    "spec-up": "^0.10.8"
  }
}
